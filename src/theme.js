import {createTheme} from "@material-ui/core";


export const theme = createTheme({
   palette: {
      primary: {
         main: "#009688"
      },
      // secondary: {
      //    main: "#3d3d3d"
      // }
   },
   myButton: {
      backgroundColor: "red",
      color: "white",
      border: "1px solid black"
   }
}) 
